Backup Use Cases

Primary Initial Backup
	I have the NAS server, saratoga 192.168.1.60/192.168.0.60 which needs to be upgraded but I want to insure that I have all the data files backedup up before the upgrade.  On the server, there are two primary mounts /mnt/saratoga-01 and /mnt/saratoga-02.  Much of the data is already exposed through nfs/smb mounts on the server. 
Unclear if backing up from the nfs mounts is sufficient
Does my “backup server” kodiak 192.168.1.61/192.168.0.61 have enough disk space?
Note that 192.168.0 is a private network between these hosts
Note that kodiak is already a staging server especially for photography - i push new folders of pictures by year/year-month-day {description}/ onto kodiak and then use the /kodiak00/bin/rsync-saratoga.sh to sync the kodiak up to saratoga.

In the future, I want a better way to backup my linux and mac hosts on a more regular basis.  I am only concerned about local machines and 2 sources for remote repositories.  I also have some legacy code repositories already on the NFS server that I want to be careful I have backups.

The backup naming for the various hosts should be {hostname}/{user}{backup} but I need a way to quickly figure out last time backups were made for a host/user.  I would expect to use rsync since I am not too concerned about being able to roll back.  ssh key sync/management may be something to think about.
Also for the user backups, todays development environments bring in a lot of tools and browsers eat disk for lunch, dinner, and snacks.  I want to skip bringing all that cruft into the backup system.  I have a script already (which i have pointed you to) /kodiak00/data-00/backups/host-backups/dev-01-cyfir/clean-home.sh  which is good for both scanning and cleaning cruft from the backup but the same details can be used in rsync to minimize what we bring over.  

I have client machines with various linux, mostly debian.  I also have a primary machine running MacOS for which i do use time machine, but since ssh would work for this system as well, but the the exclusions might be different given mac likes to do things differently. 

Since both of these cases are concerned about data and not specifically applications, restore is less important.

I do have data that relates more specifically to jobs, like utilities that I might have written to help me as well as files/folders that pertain to parts of jobs.  I think a separate effort of how to organize my “archives” better can be lined up after I know I have good backups.  

  

